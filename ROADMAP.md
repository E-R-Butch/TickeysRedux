# Roadmap

> 状态：✓ 已完成 · △ 已有基础但仍需修正 · ○ 计划 · ? 研究项，不构成版本承诺

Tickeys Redux 的目标不只是让旧代码重新运行，而是在保持本地运行、最小权限和无后台遥测的前提下，先达到原版 macOS Tickeys 1.1.0 的关键功能完整度，再提供更安全、更现代的扩展能力。

功能基线以原版官方 macOS 1.1.0 更新记录为准，而不只以公开仓库中的较旧源码快照为准：

- [原版官方更新记录](https://www.yingdev.com/projects/tickeys)
- [原版 GitHub 仓库](https://github.com/yingDev/Tickeys)

## 原则

1. 先修可靠性和发布完整性，再增加功能。
2. 原版已有的关键功能不能作为普通增强项无限后推。
3. 可以改进原版实现，但不能用“现代重写”掩盖功能回退。
4. 坚持本地运行、无遥测、无云服务、无后台更新 beacon。
5. 继续采用长期免费自签名身份；不购买 Apple Developer ID、不做 Apple 公证。这是发布政策，不是待解决的技术债。
6. App 识别使用 `NSWorkspace` 提供的前台应用 bundle ID；`CGEventTap` 只负责按键事件。

## v1.0.7 — 当前基线

### 已交付

- ✓ Apple Silicon arm64 原生应用，Rust 2024、objc2、rodio/CoreAudio
- ✓ 8 套内置音效方案
- ✓ 菜单栏和“音效 / 通用 / 关于”三标签偏好设置窗口
- ✓ 音量、音调、方案持久化基础
- ✓ Enter、Space、Backspace 独立音效映射
- ✓ 使用 Input Monitoring，不要求 Accessibility 权限
- ✓ 使用 `SMAppService` 管理开机自启
- ✓ 隐藏菜单栏图标后，再次打开 App 可显示偏好设置
- ✓ Event Tap 与睡眠唤醒的基础恢复
- ✓ CoreAudio 初始化失败后的重试，以及同步播放错误后的输出重建
- ✓ DMG、ZIP、版本、架构、部署目标、签名与校验和检查
- ✓ 本地运行，无遥测、云服务或后台更新检查

### 已知限制

- △ 授权 Input Monitoring 后，返回应用不会稳定地自动重检权限
- △ Event Tap 重新启用失败时，不会自动销毁并重建监听
- △ 菜单栏与偏好设置窗口可能显示不同状态
- △ `0.25% / 0.5% / 0.75% / 1%` 等合法低音量可能被旧版迁移规则错误放大
- △ 音调滑杆范围与音频引擎实际范围不一致
- △ 音量和音调控制命令可能在音频队列繁忙时被静默丢弃
- △ CoreAudio 自动恢复尚未覆盖真实异步设备断开测试
- △ 自定义方案需要修改源码资源并重新构建，不是最终用户导入功能
- △ About 页面尚未完整本地化，链接不可点击，许可证信息不完整
- △ 音效资产来源与再分发授权记录不完整
- △ 最低支持 macOS 13；Apple Silicon 的 macOS 11/12 尚不支持

## 下一维护版（暂定 v1.0.8）— 可靠性与发布完整性

### 输入与音频可靠性

- ○ 应用重新激活、唤醒、打开偏好设置和 Event Tap 失效时，统一重检 Input Monitoring
- ○ Event Tap 重新启用失败时自动重建监听
- ○ 显示输入监控与音频输出状态，并提供不依赖键盘权限的“测试声音”
- ○ 使用一次性版本标记修正音量迁移
- ○ 统一界面与音频引擎的音调范围
- ○ 将可靠的控制状态与可丢弃的击键播放事件分离，保证最新音量和音调最终生效
- ○ 后台解码音效方案，避免切换方案时阻塞主线程
- ○ 避免每次按键复制完整 PCM 样本

### 设置界面

- ○ 菜单栏和偏好设置共用单一状态源并相互刷新
- ○ 固定偏好设置窗口尺寸，或改用 Auto Layout 支持正确缩放
- ○ 完成本地化的 About 页面
- ○ 将原版仓库、Redux 仓库、Releases 和许可证做成可点击链接
- ○ 正确使用方案的 `display_name`

### 依赖、测试与发布

- ○ 用 `crossbeam-channel` 替换完整 `crossbeam` 依赖
- ○ 移除未使用的 `ureq`、`libc` 及不需要的音频 codec 和系统框架 feature
- ○ CI 执行格式检查、测试、Clippy、arm64 构建和全部 WAV 解码
- ○ 正式发布硬固定预期签名证书指纹
- ○ 发布前验证干净工作树、版本标签与构建提交
- ○ App Bundle 包含项目 LICENSE 和第三方声明
- ○ 补齐全部音效资产来源与许可证，或替换无法确认授权的资源
- ○ 统一 Base localization、development region 和打包语言列表

### 文档准确性

- ✓ 以官方 macOS 1.1.0 为原版功能基线
- ✓ 如实描述原版已有的设置窗口、权限流程和核心功能
- ✓ 不再把 Star Wars 方案作为 Redux 独有功能
- ✓ 区分“同步播放失败重建”与尚未验证的异步设备断开恢复
- ✓ 区分源码级自定义与最终用户可用的导入功能
- ○ 每次发布同步 README、中文 README、Roadmap、About 与发布说明

## v1.1 — 原版 macOS 1.1.0 功能对齐门槛

以下能力属于原版关键功能，不应继续后推为普通增强项。

### 已经对齐

- ✓ Star Wars 等内置音效方案
- ✓ 音量和手动音调控制
- ✓ 开机自启
- ✓ 隐藏菜单栏图标
- ✓ 睡眠唤醒基础恢复

### 必须恢复或改进

- ○ 全局启用/禁用音效
- ○ 对当前前台 App 一键禁用
- ○ App 黑名单与白名单，按 bundle ID 保存
- ○ 隐藏菜单栏图标后的全局唤出手势，提供与原版等价的恢复入口
- ○ 可选的修饰键音效
- ○ 可选的模拟按键音效
- ○ 根据打字速度自动调整音调
- ○ 可选的系统音量以上增益，并提供削波限制和明确提示

### 有意不同于原版

原版包含自动更新检查。Redux 不恢复后台更新 beacon，以维持无后台网络承诺。可以提供用户主动点击的“检查更新”或“打开 Releases”，但不得静默联网。

### 完成标准

原版官方 1.1.0 的每项用户可见核心能力，要么实现，要么提供理由明确且能力不低于原版的替代方案。

## v1.2 — 真正超越原版

### 安全的自定义音效

- ○ 从 `~/Library/Application Support/Tickeys Redux/` 加载用户方案
- ○ 通过 `NSOpenPanel` 导入单个 WAV 或完整方案
- ○ 校验格式、大小、采样率、映射和方案名称
- ○ 不修改已签名 App Bundle
- ○ 支持删除、导出、备份与恢复用户方案
- ○ 完整使用 `display_name`
- ○ 导入时记录用户提供的来源和许可证信息

### 按 App 分配方案

- ○ 在黑名单/白名单基础上，为 bundle ID 指定音效方案
- ○ 前台应用切换时更新缓存，不在每次按键时查询系统
- ○ 未安装或改名应用仍能安全保留配置
- ○ 支持“当前 App 使用此方案”的快捷操作

### 体验改进

- ○ 方案预览和独立测试声音
- ○ 音量、音调可编辑输入和合理吸附点
- ○ 首次启动引导：选择方案、测试音频、授权输入监控
- ○ 用户主动触发的更新检查
- ○ 方案管理与故障诊断页面

## 后续研究，不绑定版本

以下项目只有在存在可靠公开 API、清晰用户价值和可测试行为时，才进入正式版本：

- ? 屏幕锁定时自动静音
- ? 输出设备切换和耳机拔出策略
- ? Focus Mode、通话、全屏或演示状态感知
- ? macOS 11/12 兼容方案
- ? 更多语言；除翻译 `Localizable.strings` 外，还需更新 lproj 资源、打包脚本与语言元数据
- ? 菜单栏动画和打字进度环
- ? 演奏模式
- ? Linux 和 Windows 移植；需要分别处理输入权限、托盘、音频设备、Wayland/Win32 与安装分发，不能视为只替换 UI

不承诺通过私有 API、猜测通知或吞掉正常键盘事件来实现系统状态检测。不得为了抑制系统警告音而引入 Accessibility 权限或改变用户实际输入行为。

## 发布政策

- 正式版本使用长期免费自签名身份
- 不购买 Developer ID，不做 Apple 公证
- 文档明确说明 Gatekeeper 的首次打开步骤
- 无遥测、无云同步、无后台网络请求
- 正式产物必须能追溯到明确标签、提交、签名身份和校验和
